#category = short_term medium_term or long_term
# ROOT is faction leader, FROM is faction member who opens UI and views goals, completed/cancel/complete_effect do not have FROM scopes
faction_goal_form_central_equus_defence_district = {
	name = faction_goal_form_central_equus_defence_district_name
	description = faction_goal_form_central_equus_defence_district_desc
	category = medium_term
	group = resources_to_market_icon
	allowed = {
		OR = {
			original_tag = EQS
			original_tag = EPT
			original_tag = PGT
		}
	}
	visible = {
		FROM = {
			has_war_with = ICE
		}
	}
	completed = {
		EQS = {
			is_in_faction_with = EPT
			is_in_faction_with = PGT
		}
	}
	cancel = {
		EQS = {
			has_completed_focus = eqs_focus_central_equus_defence_district
			OR = {
				has_war_with = EPT
				has_war_with = PGT
			}
		}
	}
	complete_effect = {
		add_faction_power_projection = 500
		custom_effect_tooltip = generic_skip_one_line_tt
		custom_effect_tooltip = THE_FACTION_LEADER
		add_faction_initiative = 1
		custom_effect_tooltip = generic_skip_one_line_tt
		every_faction_member = { 
			tooltip = INFLUENCE_TOP_THREE
			visible_when_empty = yes
			limit = {
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = {
		base = 1
	}
}
faction_goal_defeat_ICE = {
	name = faction_goal_defeat_ICE_name
	description = faction_goal_defeat_ICE_desc
	category = long_term
	group = resources_to_market_icon
	visible = {
		FROM = {
			has_war_with = ICE
		}
	}
	completed = {
		ICE = {
			exists = no
		}
	}
	complete_effect = {
		add_faction_power_projection = 500
		custom_effect_tooltip = generic_skip_one_line_tt
		custom_effect_tooltip = THE_FACTION_LEADER
		add_faction_initiative = 1
		custom_effect_tooltip = generic_skip_one_line_tt
		every_faction_member = { 
			tooltip = INFLUENCE_TOP_THREE
			visible_when_empty = yes
			limit = {
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = {
		base = 1
	}
}
