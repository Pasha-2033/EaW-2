#category = short_term medium_term or long_term
# ROOT is faction leader, FROM is faction member who opens UI and views goals, completed/cancel/complete_effect do not have FROM scopes
faction_goal_resource_control_0 = {
	name = resource_control_goal_name
	description = resource_control_goal_desc
	category = long_term
	group = resources_to_market_icon
	completed = {
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = oil
			amount > 9
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = coal
			amount > 49
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = crystals
			amount > 24
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = bauxite
			amount > 24
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = iron
			amount > 49
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = scheelitis
			amount > 24
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = chromite
			amount > 24
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = food
			amount > 74
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
	}
	complete_effect = {
		add_faction_power_projection = 500
		custom_effect_tooltip = generic_skip_one_line_tt
		custom_effect_tooltip = THE_FACTION_LEADER
		add_faction_initiative = 1
		custom_effect_tooltip = generic_skip_one_line_tt
		every_faction_member = { 
			tooltip = INFLUENCE_TOP_THREE
			visible_when_empty = yes
			limit = {
				faction_influence_rank < 4
			}
			add_political_power = 100
		}
	}
	ai_will_do = {
		base = 1
	}
}
faction_goal_atomic_power = {
	name = faction_goal_atomic_power_name
	description = faction_goal_atomic_power_desc
	category = long_term
	group = FOCUS_FILTER_RESEARCH
	visible = {
		FROM = {
			has_tech = atomic_research
		}
	}
	completed = {
		has_resources_in_collection = {
			collection = collection:faction_members
			resource = nuclear_fuel_primary
			amount > 9
			extracted = yes # (optional, default: no) checks extracted amount instead of country balance
		}
		count_in_collection = {
			collection = collection:faction_members
			buildings = { nuclear_reactor_exploratory }
			size > 2
		}
		count_in_collection = {
			collection = collection:faction_members
			buildings = { nuclear_reactor }
			size > 0
		}
		count_in_collection = {
			collection = collection:faction_members
			buildings = { nuclear_reactor_heavy_water }
			size > 0
		}
	}
	complete_effect = {
		every_faction_member = {
			add_political_power = 100
		}
	}
}
faction_goal_thermonuclear_miracle = {
	name = faction_goal_thermonuclear_miracle_name
	description = faction_goal_thermonuclear_miracle_desc
	category = long_term
	group = FOCUS_FILTER_RESEARCH
	visible = {
		FROM = {
			has_tech = atomic_research
		}
	}
	completed = {
		count_in_collection = {
			collection = collection:faction_members
			buildings = { thermonuclear_reactor }
			size > 0
		}
	}
	complete_effect = {
		every_faction_member = {
			add_political_power = 100
		}
	}
}