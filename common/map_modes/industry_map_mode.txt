scripted_map_modes = {
	industry_map_mode = {
		top = {
            type = state
            color = {
				if = {
					limit = {
						OR = {
							is_ally_with = FROM.owner
							intel_level_over = {
								target = FROM.owner
								civilian_intel > 0.75
							}
						}
					}
					FROM = {
						set_temp_variable = {
							count = {
								value = building_level@aluminium_plant
								add = building_level@steel_plant
								add = building_level@tungsten_plant
								add = building_level@chromium_plant
							}
						}
						#results
						if = {
							limit = {
								check_variable = {
									count = 0
								}
							}
							set_temp_variable = { alpha = 0 }
						}
						else = {
							set_temp_variable = {
								red = {
									value = 0.957
									multiply = building_level@aluminium_plant
									add = {
										value = 0.258
										multiply = building_level@steel_plant
									}
									add = {
										value = 0.851
										multiply = building_level@tungsten_plant
									}
									divide = count
								}
							}
							set_temp_variable = {
								green = {
									value = 0.4
									multiply = building_level@aluminium_plant
									add = {
										value = 0.957
										multiply = building_level@steel_plant
									}
									add = {
										value = 0.258
										multiply = building_level@tungsten_plant
									}
									add = {
										value = 0.113
										multiply = building_level@chromium_plant
									}
									divide = count
								}
							}
							set_temp_variable = {
								blue = {
									value = 0.258
									multiply = building_level@aluminium_plant
									add = {
										value = 0.41
										multiply = building_level@steel_plant
									}
									add = {
										value = 0.957
										multiply = building_level@tungsten_plant
									}
									add = {
										value = 0.758
										multiply = building_level@chromium_plant
									}
									divide = count
								}
							}
							set_temp_variable = { alpha = 1 }
						}
					}
				}
				else = {
					set_temp_variable = { alpha = 0 }
				}
				set_temp_variable = { thickness = 0.8 }
				set_temp_variable = { highlighted = 0 }
			}
        }
		bottom = {
			type = none
		}
        far_text = country
        near_text = state
    }
}